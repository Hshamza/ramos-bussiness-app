import { AnimationTriggerDirective } from './animation-trigger.directive';

describe('AnimationTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
